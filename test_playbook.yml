- name: Test galaxy_service collection
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Test module - validate Galaxy service
      galaxy_service.galaxy_service.galaxy_service:
        galaxy_url: "http://localhost:8080"
        username: "admin"
        password: "admin"
        action: validate

    - name: Test module - test complete workflow
      galaxy_service.galaxy_service.galaxy_service:
        galaxy_url: "http://localhost:8080"
        username: "admin"
        password: "admin"
        action: test
