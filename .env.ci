# CI Environment Configuration
# This file is used for GitHub Actions CI/CD pipelines

# Django Settings
DEBUG=True

# PostgreSQL Settings
POSTGRESQL_USER=galaxy
POSTGRESQL_PASSWORD=galaxy
POSTGRESQL_DATABASE=galaxy
POSTGRESQL_HOST=postgres
POSTGRESQL_PORT=5432

# Redis Settings
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=galaxy

# Galaxy Admin Settings
GALAXY_ADMIN_USER=admin
GALAXY_ADMIN_PASSWORD=admin

# Pulp Secret Key
PULP_SECRET_KEY=ci-test-secret-key

# Galaxy API
GALAXY_API_HOSTNAME=http://localhost:8000
GALAXY_CONTENT_HOSTNAME=http://localhost:24816
GALAXY_WEB_HOSTNAME=http://localhost:8080
CONTENT_ORIGIN=http://localhost:8080

# Feature Flags
EE_ENABLED=False
AI_DENY_INDEX=False

# Logging
LOG_LEVEL=INFO

# Backup Configuration (CI specific)
BACKUP_RETENTION_DAYS=1
